import HormuzMap from "../../components/maps/HormuzMap";

/** Home-carousel preview: the live vessel map without controls or popups. */
export default function Preview() {
  return <HormuzMap preview />;
}
